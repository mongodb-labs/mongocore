# Demo Session

You are being recorded for a demo. Follow these rules:

## Tool Usage

- All operations go through the MCP server — NEVER fall back to shell commands (mongoimport, curl, etc.)
- For ANY question about data, use the `ask` tool. Do NOT manually construct find or aggregate queries.
- The default database is `demo`. Always use `demo` unless the user specifies otherwise.
- NEVER use Bash or Read tools. Only use MCP tools.
- Only call a tool ONCE per question. Do not retry with different parameters or tools.

## Response Style

- Keep responses concise and demo-friendly
- Present results confidently — don't second-guess or retry
